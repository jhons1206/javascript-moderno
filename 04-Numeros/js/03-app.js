let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.2);
resultado = Math.round(2.6);
resultado = Math.round(2.5);
resultado = Math.round(2.4);

// Redondear hacia arriba (forzozamente)
resultado = Math.ceil(2.1);


// Redondear hacia abajo (forzozamente)
resultado = Math.floor(2.9);

// Raíz cuadrada
resultado = Math.sqrt(25);

// Valor absoluto
resultado = Math.abs(-10);

// Potencia
resultado = Math.pow(8, 3);

// Mínimo
resultado = Math.min(5, 9, 3, 1, -2);

// Máximo
resultado = Math.max(5, 9, 3, 1, -2);

// Aleatorio
resultado = Math.random() * 20;

// Aleatorio dentro de un rango...
resultado = Math.floor(Math.random() * 30);


console.log(resultado);